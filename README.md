# SHUTDOWN-REBOOT
Simple as the batch scripting language is here is some examples that it still going strong. Imagine windows 12 with a stable release and additional batch scripting build commands. Generally speaking batch scripting is indeed a language for the old guys: GOTO. Beware though of GOTO
